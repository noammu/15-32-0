import { createBoard } from '@wixc3/react-board';
import { Sanitationnnnnn } from '../../../components/sanitationnnnnn/sanitationnnnnn';

export default createBoard({
    name: 'Sanitationnnnnn',
    Board: () => <Sanitationnnnnn />,
    isSnippet: true,
    tags: ['sdsd'],
});
